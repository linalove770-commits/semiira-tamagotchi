(() => {'use strict';const dialog=document.getElementById('side-drawer');let opener=null;function select(id){for(const el of dialog.querySelectorAll('.needs-card,.progress-card,#customize,.save-panel,#pet-wardrobe'))el.hidden=el.id!==id;for(const b of dialog.querySelectorAll('[data-drawer]'))b.setAttribute('aria-pressed',String(b.dataset.drawer===id));const customize=document.getElementById('customize');if(customize)customize.open=id==='customize';dialog.querySelector('.drawer-content').scrollTop=0;}function open(id='customize'){select(id);if(!dialog.open){opener=document.activeElement;dialog.showModal();document.body.classList.add('drawer-open');dialog.dispatchEvent(new CustomEvent('tama-drawer-open',{bubbles:true}));}document.getElementById('close-drawer').focus();}dialog.addEventListener('close',()=>{document.body.classList.remove('drawer-open');dialog.dispatchEvent(new CustomEvent('tama-drawer-close',{bubbles:true}));opener?.focus({preventScroll:true});});dialog.addEventListener('click',e=>{if(e.target===dialog){const r=dialog.getBoundingClientRect();if(e.clientX<r.left||e.clientX>r.right||e.clientY<r.top||e.clientY>r.bottom)dialog.close();}});document.getElementById('close-drawer').onclick=()=>dialog.close();document.getElementById('open-drawer').onclick=()=>open();dialog.querySelectorAll('[data-drawer]').forEach(b=>b.onclick=()=>select(b.dataset.drawer));window.TamaDrawer={open,isOpen:()=>dialog.open};})();
